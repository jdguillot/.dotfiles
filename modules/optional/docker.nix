{ ... }:
{
  ###### Docker
  virtualisation.docker = {
    enable = true;
    enableOnBoot = true;
  };
  # virtualisation.docker.rootless = {
  #   enable = true;
  #   setSocketVariable = true;
  # };

}
